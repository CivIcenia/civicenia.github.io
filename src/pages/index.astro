---
import { Astros } from "@helpers";

// Use a relative redirect so the root of a project-site (e.g.
// `https://<user>.github.io/civicenia.github.io/`) redirects to
// `.../civicenia.github.io/laws/` rather than the domain root.
return Astros.redirect("laws/");
---
